import React from 'react';
import { Typography } from '@material-ui/core';

const Home = () => {
  return (
    <div>
      <Typography variant='h2'>Home</Typography>
    </div>
  );
};

export default Home;
